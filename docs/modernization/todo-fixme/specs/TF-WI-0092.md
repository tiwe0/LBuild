---
title: EHCI async doorbell mutex scope
status: active
owner: io-platform
last-verified: 2026-09-01
verified-against: working-tree
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0092: EHCI async doorbell mutex scope

## Current boundary

The baseline marker at `Lambda64/drivers/usb/ehci-intel.lisp:291` remains an explicit
implementation boundary. wait-for-async-doorbell currently holds the controller mutex while polling hardware. Narrowing the scope requires an interrupt-safe state machine and hardware evidence; the marker remains active.

## Required evidence

Add host and hardware-backed regression coverage for the success, ordering,
and failure paths, then remove the marker only after the implementation and
controller behavior are verified together.
