---
title: Report SETF and CAS expanders in DESCRIBE
status: active
owner: runtime
last-verified: 2026-09-01
verified-against: working-tree
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0361 — `DESCRIBE` 的 SETF/CAS expander 报告

- **基线标记：** `TF-occ-02292bef4c72c8ac169a714b`
- **代码事实源：** `Lambda64/system/describe.lisp:42-45`
- **风险：** high
- **要求测试层：** host、cold-image、integration

## 目标

描述符号时，同时检查其规范函数名 `(SETF symbol)` 与 `(CAS symbol)`，并在存在时输出
对应函数信息。两种 expander 使用与普通函数相同的 `finfo` 路径，以保持 compiler-macro
和 inline 元数据的报告行为一致。

## 先测试证据

新增 `Lambda64/tests/host/test-system-describe-setf-cas.sh`。测试从生产源提取符号专用
`describe-object` 方法，在 host fixture 中注册 SETF/CAS 函数并验证输出；变异负例移除
SETF 报告调用，必须被测试拒绝。

实现前运行：

```sh
bash Lambda64/tests/host/test-system-describe-setf-cas.sh
```

旧代码已经具备两个调用，故本次修复清理过时 TODO 标记并以回归测试锁定现有行为。

## 验收标准

1. 符号描述同时探测 SETF 与 CAS 函数名。
2. expander 信息沿用 `finfo`，不绕过已有元数据输出逻辑。
3. 普通符号、宏及 plist/声明输出行为保持不变。
4. cold image 与 QEMU integration 待完整环境验收，工作项保持 `active`。

## 当前证据

```sh
bash -n Lambda64/tests/host/test-system-describe-setf-cas.sh
bash Lambda64/tests/host/test-system-describe-setf-cas.sh
```

两项均已通过；后者同时确认 SETF 报告变异被拒绝。尚未执行 cold image 或 QEMU
integration，故本工作项保持 `active`。
