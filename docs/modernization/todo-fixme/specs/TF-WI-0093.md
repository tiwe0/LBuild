---
title: EHCI port debounce semantics
status: active
owner: io-platform
last-verified: 2026-09-01
verified-against: working-tree
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0093: EHCI port debounce semantics

## Current boundary

The baseline marker at `Lambda64/drivers/usb/ehci-intel.lisp:426` remains an explicit
implementation boundary. debounce-port is an empty protocol hook. Implementing debounce requires EHCI timing and companion-controller behavior not covered by host tests, so this marker remains active.

## Required evidence

Add host and hardware-backed regression coverage for the success, ordering,
and failure paths, then remove the marker only after the implementation and
controller behavior are verified together.
