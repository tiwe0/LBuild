---
title: Complete EQUAL hash-key circularity checks
status: active
owner: runtime
last-verified: 2026-09-01
verified-against: working-tree
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0396 — `EQUAL` 哈希键的完整 circularity 检查

- **基线标记：** `TF-occ-4fbb775ed9489a42e41b44db`
- **代码事实源：** `Lambda64/system/hash-table.lisp:39-86`
- **风险：** high
- **要求测试层：** host、cold-image、integration

## 目标

`object-hash-gc-invariant-under-test` 的 `equal` 分支需要沿 cons 的完整 cdr 链检查，
并通过 tortoise/hare（fast/slow）指针拒绝 circular list。每个 car 仍使用有界递归
深度检查，避免嵌套结构导致无界递归；proper list、dotted list 及其嵌套值保持既有
哈希不变性判断。

## 先测试证据

新增 `Lambda64/tests/host/test-system-hash-table-circularity.sh`，检查 `frob-list`、
完整 cdr 遍历及 fast/slow 相遇保护，并确认移除相遇保护的变异会被拒绝。

## 验收标准

1. EQUAL 哈希键的 cdr 链无论长度如何都被遍历，循环链在有限步骤内判定为不稳定。
2. 嵌套 car 结构仍受递归深度上限约束，不会因深层树形数据耗尽栈空间。
3. proper、dotted 和非 cons 对象继续按既有 immediate/number/string 等规则判定。
4. codegen、cold image 和 integration 验证待完整构建环境恢复后执行，工作项保持
   `active`。

## 当前证据

```sh
bash -n Lambda64/tests/host/test-system-hash-table-circularity.sh
bash Lambda64/tests/host/test-system-hash-table-circularity.sh
```

host 合约测试通过，并确认删除 fast/slow 相遇保护的变异会失败。完整 codegen、cold
image 与 integration 尚未运行。
