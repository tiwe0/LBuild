---
title: EHCI qTD condition-code handling
status: active
owner: io-platform
last-verified: 2026-09-01
verified-against: working-tree
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0096: EHCI qTD condition-code handling

## Current boundary

The baseline marker at `Lambda64/drivers/usb/ehci-intel.lisp:685` remains an explicit
implementation boundary. Bulk completion still needs condition-code decoding and error policy. Higher-level retry semantics and controller status mappings are not established by current fixtures.

## Required evidence

Add host and hardware-backed regression coverage for the success, ordering,
and failure paths, then remove the marker only after the implementation and
controller behavior are verified together.
