---
title: Define ARM64 trampoline cache-coherence boundary
status: active
owner: io-platform
last-verified: 2026-09-01
verified-against: working-tree
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0218: Define ARM64 trampoline cache-coherence boundary

## Scope

Close the marker on `%arm64-sync-icache` in `Lambda64/supervisor/arm64/cpu.lisp` without claiming an unimplemented
architecture or hardware protocol.

## Contract

The implementation retains its current synchronization and interrupt
boundaries.  The surrounding comment records the precondition or deferred
work explicitly: migration must be constrained by a future scheduler-aware
quiescence protocol; debug-button entry is currently BSP/IRQ serialized; and
architecture-specific cache, stack-alignment, or TLB behavior remains bounded
to the documented safe path.

## Verification

`Lambda64/tests/host/test-supervisor-cpu-marker-boundaries.sh` checks that the
marker is closed and the boundary language remains present.
