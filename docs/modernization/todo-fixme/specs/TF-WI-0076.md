---
title: Intel GMA stereo mode handling
status: active
owner: io-platform
last-verified: 2026-09-01
verified-against: git:5422d69249c2104ff0ab1feff20806013cdba635
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0076: Intel GMA stereo mode handling

## Scope

This specification resolves the corresponding baseline marker in
`Lambda64/drivers/intel-gma.lisp` without expanding the supported PCI device
list or weakening modeset safety.

## Required behavior

All EDID detailed-timing stereo encodings are decoded to distinct symbolic modes. Because GMA 950 has no supported stereo pipe programming path, mode validation rejects stereo modes rather than silently programming a non-stereo scanout; saved driver-owned modes are therefore non-stereo.

## Acceptance evidence

The focused host fixture covers all six defined nonzero EDID stereo encodings and verifies explicit rejection at the hardware boundary. `Lambda64/tests/host/test-intel-gma-timings.sh` loads the repository
driver with host MMIO stubs and executes its pure timing and register semantics.
Against the pre-change source, the fixture fails because the named sync
constructor and other required behavior are absent; against this implementation
it passes.

This work item remains `active` until a GMA 950 guest/hardware run records PLL
lock, two-frame shutdown, register readback, and stable physical scanout across
progressive/interlaced EDID modes. Host tests do not claim those electrical or
timing properties.
