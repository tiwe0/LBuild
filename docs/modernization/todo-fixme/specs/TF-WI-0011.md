---
title: Make ARM64 register swaps GC-safe
status: active
owner: compiler
last-verified: 2026-09-01
verified-against: working-tree
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0011: Make ARM64 register swaps GC-safe

## Scope

`Lambda64/compiler/backend/arm64/codegen.lisp:510`.

## Current boundary

The XOR swap path aliases live values and can hide a GC root between safepoints. Replace with a temporary register or stack spill and prove root visibility under allocation and safepoint pressure before removing the marker.

## Required evidence

Add focused code-generation tests plus the relevant host/guest integration test before removing the marker. The test must cover the architectural boundary, unchanged behavior for existing inputs, and failure/overflow behavior where applicable.
