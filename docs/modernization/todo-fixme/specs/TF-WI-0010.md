---
title: Support large ARM64 argument counts
status: active
owner: compiler
last-verified: 2026-09-01
verified-against: working-tree
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0010: Support large ARM64 argument counts

## Scope

`Lambda64/compiler/backend/arm64/codegen.lisp:365`.

## Current boundary

Argument checks currently encode counts as SUBS immediates. Counts above the architectural immediate range require a literal or register path while preserving all required/optional/rest branches; retain marker until generated code is tested at and above the limit.

## Required evidence

Add focused code-generation tests plus the relevant host/guest integration test before removing the marker. The test must cover the architectural boundary, unchanged behavior for existing inputs, and failure/overflow behavior where applicable.
