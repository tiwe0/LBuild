---
title: Export logical binary and generic variants
status: active
owner: runtime
last-verified: 2026-09-01
verified-against: working-tree
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0400 — 导出 logical 的 binary/generic 变体

- **基线标记：** `TF-occ-3e53570c81caf6c1528c2e2b`
- **代码事实源：** `Lambda64/system/numbers/logical.lisp:113-126`
- **风险：** medium
- **要求测试层：** host、cold-image、integration

## 目标

`mezzano.internals.numbers.logical` 除标准 `logand`、`logior`、`logxor` 外，还导出
编译器和数值运行时使用的 `binary-*` 与 `generic-*` 入口。导出符号与
`mezzano.internals` 中的实现保持同一 identity，避免创建包装函数或重复逻辑。

## 先测试证据

新增 `Lambda64/tests/host/test-system-logical-exports.sh`，验证生产源导入六个内部入口
并在 logical 包中导出；变异负例移除 `generic-logand` 导出，必须被测试拒绝。

实现前运行：

```sh
bash Lambda64/tests/host/test-system-logical-exports.sh
```

原代码没有导入/导出这些变体；本次补充包边界声明并移除过时 TODO。

## 验收标准

1. `binary-logand`、`generic-logand`、`binary-logior`、`generic-logior`、
   `binary-logxor`、`generic-logxor` 均从 logical 包可见且为原实现符号。
2. 标准逻辑运算及 compiler-macro 行为保持不变。
3. 不引入包装层或额外依赖。
4. cold image 与 QEMU integration 待完整环境验收，工作项保持 `active`。

## 当前证据

```sh
bash -n Lambda64/tests/host/test-system-logical-exports.sh
bash Lambda64/tests/host/test-system-logical-exports.sh
```

两项均已通过；后者同时确认导出契约变异被拒绝。尚未执行 cold image 或 QEMU
integration，故本工作项保持 `active`。
