---
title: USB transfer-complete shared ownership
status: active
owner: io-platform
last-verified: 2026-09-01
verified-against: working-tree
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0095: USB transfer-complete shared ownership

## Current boundary

The baseline marker at `Lambda64/drivers/usb/ehci-intel.lisp:654` remains an explicit
implementation boundary. transfer-complete is duplicated across HCDs. Moving it to usb-defs requires reconciling package exports and OHCI/EHCI event semantics; retain the marker until a shared contract is tested.

## Required evidence

Add host and hardware-backed regression coverage for the success, ordering,
and failure paths, then remove the marker only after the implementation and
controller behavior are verified together.
