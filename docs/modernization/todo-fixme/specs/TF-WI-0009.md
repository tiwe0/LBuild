---
title: Preserve ARM64 stack GC layout density
status: active
owner: compiler
last-verified: 2026-09-01
verified-against: working-tree
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0009: Preserve ARM64 stack GC layout density

## Scope

`Lambda64/compiler/backend/arm64/codegen.lisp:51`.

## Current boundary

Stack layout ordering and GC bitvector trimming can change root-slot numbering and frame metadata. Keep marker active until an allocator+GC integration fixture proves equivalent root visibility, slot offsets, and debug locations.

## Required evidence

Add focused code-generation tests plus the relevant host/guest integration test before removing the marker. The test must cover the architectural boundary, unchanged behavior for existing inputs, and failure/overflow behavior where applicable.
