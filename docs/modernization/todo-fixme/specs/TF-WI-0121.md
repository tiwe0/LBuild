---
title: USB HCD access guard coverage
status: active
owner: io-platform
last-verified: 2026-09-01
verified-against: working-tree
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0121: USB HCD access guard coverage

## Current boundary

The baseline marker at `Lambda64/drivers/usb/usb-driver.lisp:19` remains an explicit
implementation boundary. with-hcd-access must surround every register and DMA reference. Auditing and adding guards requires controller-specific critical-section tests; retain the marker until that audit is complete.

## Required evidence

Add host and hardware-backed regression coverage for the success, ordering,
and failure paths, then remove the marker only after the implementation and
controller behavior are verified together.
