---
title: EHCI device control-event ownership
status: active
owner: io-platform
last-verified: 2026-09-01
verified-against: working-tree
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0091: EHCI device control-event ownership

## Current boundary

The baseline marker at `Lambda64/drivers/usb/ehci-intel.lisp:236` remains an explicit
implementation boundary. The EHCI-specific control-event slot remains on ehci-device until usb-device can own it without changing initialization and teardown ordering. No safe move is proven by the current host fixtures.

## Required evidence

Add host and hardware-backed regression coverage for the success, ordering,
and failure paths, then remove the marker only after the implementation and
controller behavior are verified together.
