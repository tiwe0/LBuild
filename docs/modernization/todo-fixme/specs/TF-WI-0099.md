---
title: Report HID mouse probe failures with report-id context
status: active
owner: runtime
last-verified: 2026-09-01
verified-against: working-tree
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0099 — HID 鼠标探测错误诊断

- **基线标记：** `TF-occ-62786244b49fc76d064795e4`
- **代码事实源：** `Lambda64/drivers/usb/hid-mouse.lisp:190-202`
- **风险：** high
- **要求测试层：** host、hardware

## 目标

当 HID 鼠标报告描述符在首个字段之后出现 `:report-id` 时，探测失败日志必须明确
说明原因并带出实际 report id，然后继续走既有 `:probe-failed` 控制流。

## 先测试证据

新增 `Lambda64/tests/host/test-driver-usb-hid-mouse-errors.sh`，检查诊断调用、report-id
上下文和失败控制流；变异负例移除诊断调用，必须被测试拒绝。实现前测试因 TODO 标记
而失败。

## 验收标准

1. 失败日志包含“report id came after first field”及实际 report id。
2. 探测仍抛出 `:probe-failed :failed`，不改变错误处理语义。
3. 在 `hardware-usb-hid-mouse` 资源可用时完成真实设备回归；当前工作项保持 `active`。

## 当前证据

```sh
bash -n Lambda64/tests/host/test-driver-usb-hid-mouse-errors.sh
bash Lambda64/tests/host/test-driver-usb-hid-mouse-errors.sh
```

上述 host 契约与变异测试已通过；USB HID 硬件验证尚未执行。
