---
title: EHCI periodic frame table
status: active
owner: io-platform
last-verified: 2026-09-01
verified-against: working-tree
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0098: EHCI periodic frame table

## Current boundary

The baseline marker at `Lambda64/drivers/usb/ehci-intel.lisp:1090` remains an explicit
implementation boundary. init-periodic-frame-list currently installs terminated entries only. Building a real periodic schedule requires interrupt endpoint scheduling and hardware timing evidence.

## Required evidence

Add host and hardware-backed regression coverage for the success, ordering,
and failure paths, then remove the marker only after the implementation and
controller behavior are verified together.
