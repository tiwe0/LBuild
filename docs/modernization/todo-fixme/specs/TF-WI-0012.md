---
title: Remove unsafe ARM64 swap implementation
status: active
owner: compiler
last-verified: 2026-09-01
verified-against: working-tree
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0012: Remove unsafe ARM64 swap implementation

## Scope

`Lambda64/compiler/backend/arm64/codegen.lisp:512`.

## Current boundary

The second swap marker emphasizes that the current implementation is not an acceptable long-term primitive. Keep it paired with TF-WI-0011 until all register classes and allocator constraints have executable coverage.

## Required evidence

Add focused code-generation tests plus the relevant host/guest integration test before removing the marker. The test must cover the architectural boundary, unchanged behavior for existing inputs, and failure/overflow behavior where applicable.
