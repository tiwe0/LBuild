---
title: Use transfer-width-scaled ARM64 memory immediates
status: active
owner: compiler
last-verified: 2026-09-01
verified-against: working-tree
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0016: Use transfer-width-scaled ARM64 memory immediates

## Scope

`Lambda64/compiler/backend/arm64/memory.lisp:14`.

## Current boundary

The effective-address fast path only recognizes signed six-bit indices although load/store encodings permit wider scaled offsets. Broaden selection per transfer width without changing unscaled access semantics; retain marker pending per-width boundary tests.

## Required evidence

Add focused code-generation tests plus the relevant host/guest integration test before removing the marker. The test must cover the architectural boundary, unchanged behavior for existing inputs, and failure/overflow behavior where applicable.
