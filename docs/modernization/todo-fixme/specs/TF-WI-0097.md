---
title: Clarify EHCI control-transfer buffer conventions
status: active
owner: runtime
last-verified: 2026-09-01
verified-against: working-tree
review-cycle: 30d
source-of-truth: code
---

# TF-WI-0097 — EHCI 控制传输注释契约

- **基线标记：** `TF-occ-a817732f16296be6f4910d15`
- **代码事实源：** `Lambda64/drivers/usb/ehci-intel.lisp:700-716`
- **风险：** high
- **要求测试层：** host、hardware

## 目标

将控制端点接收路径的历史疑问注释改为明确契约：无数据的 `set address`、
`set configuration` 请求使用零长度和一个字节的 scratch buffer；不改变传输实现。

## 先测试证据

新增 `Lambda64/tests/host/test-driver-ehci-control-comment.sh`，检查注释包含上述
约定且不再保留 TODO；变异负例修改“无数据”语义，必须被测试拒绝。实现前测试因
旧 TODO 而失败。

## 验收标准

1. 注释与当前控制传输调用约定一致。
2. 不改变 `control-receive-data` 的运行时行为。
3. 在 `hardware-usb-ehci-intel` 资源可用时完成控制请求回归；当前工作项保持 `active`。

## 当前证据

```sh
bash -n Lambda64/tests/host/test-driver-ehci-control-comment.sh
bash Lambda64/tests/host/test-driver-ehci-control-comment.sh
```

上述 host 契约与变异测试已通过；EHCI 硬件验证尚未执行。
